# Edit paths, then install with: crontab deploy/cron.example
# Runs daily at 03:30 server local time. Data is stored in a sibling /opt/data directory.
30 3 * * * mkdir -p /opt/data/_logs && cd /opt/down_obeservation && /usr/bin/env bash scripts/run_daily.sh >> /opt/data/_logs/cron.log 2>&1
