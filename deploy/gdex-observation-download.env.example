# Copy to ~/.config/gdex-observation-download.env and edit for your server.

# Repository checkout path. The install script fills the systemd unit with this path.
# APP_DIR=/opt/down_obeservation

# Keep data outside the git checkout on production servers.
GDEX_DATA_ROOT=/data/gdex
GDEX_STATE_DIR=/data/gdex_state
GDEX_LOG_DIR=/data/gdex_logs

# Requested target year.
GDEX_YEAR=2026

# UCAR recommends avoiding aggressive parallel downloads. Keep this below 10.
GDEX_MAX_WORKERS=2
GDEX_MAX_DEPTH=8
GDEX_MAX_PAGES=20000

# Optional Python interpreter.
# PYTHON_BIN=/usr/bin/python3
